<div class="my-3 col-sm-8 col-sm-offset-2">
  <% @articles.each do |article| %>
    <div class="">
      <h3><%= link_to article.title, article_path(article), class: "link-articles" %></h2>
      <p><%= "#{article.content.first(100)}..." %></p>
    </div>
  <% end %>
</div>
